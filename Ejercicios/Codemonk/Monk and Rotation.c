#include <stdio.h>

int main(){
	int T,N,K;
	scanf("%d", &T);     
    scanf("%d %d", &N, &K); 
    int A[N],i;   
    for(i=0;i<N;i++)
    {
        scanf("%d", &A[i]); 
    }
    for(i=0;i<K;i++)
    {
        

    }
     
         	                  // Reading input from STDIN
	printf("Input number is %d.\n", A[1]);       // Writing output to STDOUT
}
