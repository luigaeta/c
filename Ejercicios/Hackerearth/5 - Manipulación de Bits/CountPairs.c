#include <stdio.h>
#define size==N

int main(){
int T, N,i,j,A;
scanf("%d",&T);
for(i=0;i<T;i++)
{
    scanf("%d",&N);
    unsigned long long int** A;
    A = (int*)malloc(N * sizeof(int));
    for(j=0;j<N;j++)
    {
        scanf("%d",&A[j]);
    }
}
}